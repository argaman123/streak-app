import { Injectable } from '@angular/core';

/**
 * App is light-mode-only. This service is kept as a no-op stub so existing
 * imports stay valid; future preferences (sounds, vibration etc.) can land here.
 */
@Injectable({ providedIn: 'root' })
export class SettingsService {}
