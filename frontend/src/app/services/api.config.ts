// Base URL for our backend.
// In dev (`ng serve`) the Angular app runs on a different port from the Node server.
// In production (after `ng build`) the Node server hosts the static files too,
// so '/api' works directly. To change the URL later, edit just this one file.
export const API_BASE = 'https://learn-api.argamate.com/api';
